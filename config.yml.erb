---
:base_path: "/var/lib/gemstash"
:protected_fetch: false # do not protect private gems
<% if ENV['DB_HOST'] %>
:db_adapter: mysql2
:db_url: "mysql2://<%= ENV["DB_USER"] %>:<%= ENV["DB_PASSWORD"] %>@<%= ENV["DB_HOST"] %>/<%= ENV["DB_NAME"] %>"
:db_connection_options:
  :connect_timeout: 10
  :read_timeout: 5
  :reconnect: true
  :timeout: 30
<% end %>
<% if ENV['MEMCACHED_SERVERS'] %>
:cache_type: memcached
:memcached_servers: <%= ENV['MEMCACHED_SERVERS'] %>
<% end %>
